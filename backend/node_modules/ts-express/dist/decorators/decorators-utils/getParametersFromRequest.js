"use strict";
function getParametersFromRequest(req, res, parametersSettings) {
    return parametersSettings.map((ps) => {
        switch (ps.type) {
            case 'fromBody':
                return req.body;
            case 'fromQuery':
                return ps.paramType(req.query[ps.paramName]);
            case 'fromUrl':
                return ps.paramType(req.params[ps.paramName]);
            case 'request':
                return req;
            case 'response':
                return res;
            case 'inject':
                return ps.getFunction(req.container);
            default:
                throw new Error(`Unable to get parameter ${JSON.stringify(ps)}`);
        }
    });
}
exports.getParametersFromRequest = getParametersFromRequest;
//# sourceMappingURL=getParametersFromRequest.js.map