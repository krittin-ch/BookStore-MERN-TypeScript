"use strict";
const containerMiddleware_1 = require("./containerMiddleware");
const bodyParser = require("body-parser");
let app;
function setApp(expressApp, swaggerJson) {
    if (app) {
        throw new Error('App is already set');
    }
    app = expressApp;
    app.use(containerMiddleware_1.containerMiddleware);
    app.use(bodyParser.json());
    if (swaggerJson) {
        app.get('/swagger.json', (req, res) => {
            res.status(200).json(swaggerJson);
        });
    }
}
exports.setApp = setApp;
function getApp() {
    if (!app) {
        throw new Error('App must be set in order to get it');
    }
    return app;
}
exports.getApp = getApp;
//# sourceMappingURL=appContainer.js.map